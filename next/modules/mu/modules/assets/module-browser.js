(function(){'use strict';function init(root){const browser=root&&root.querySelector?root.querySelector('[data-afcn-module-browser]'):null;if(!browser||browser.dataset.afcnWired)return;browser.dataset.afcnWired='1';const filters=Array.from(browser.querySelectorAll('[data-afcn-module-filter]'));const cards=Array.from(browser.querySelectorAll('[data-afcn-module-card]'));const search=browser.querySelector('[data-afcn-module-search]');const empty=browser.querySelector('[data-afcn-module-empty]');let active='all';function apply(){const query=(search?search.value:'').trim().toLowerCase();let shown=0;cards.forEach(card=>{const groups=(card.dataset.afcnGroups||'').split(/\s+/);const haystack=(card.dataset.afcnSearch||'').toLowerCase();const visible=groups.includes(active)&&(!query||haystack.includes(query));card.hidden=!visible;if(visible)shown++});if(empty)empty.hidden=shown>0}filters.forEach(button=>{button.addEventListener('click',()=>{active=button.dataset.afcnModuleFilter||'all';filters.forEach(item=>item.classList.toggle('is-active',item===button));apply()})});if(search)search.addEventListener('input',apply);browser.addEventListener('click',event=>{const button=event.target.closest('[data-afcn-open-module]');if(!button||!window.AirfiberNext)return;const moduleId=button.dataset.afcnOpenModule;if(!moduleId)return;if(window.location.hash!=='#'+moduleId)history.pushState(null,'','#'+moduleId);window.AirfiberNext.loadModule(moduleId,false)});apply()}document.addEventListener('afcn:module:loaded',event=>{if(event.detail&&event.detail.id==='modules')init(document.getElementById('afcn-module-stage')||document)});const stage=document.getElementById('afcn-module-stage');if(stage&&window.MutationObserver){new MutationObserver(()=>init(stage)).observe(stage,{childList:true})}init(document)})();
